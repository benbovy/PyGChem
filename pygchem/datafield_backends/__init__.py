# -*- coding: utf-8 -*-

"""
Backends used to read/write geoschem datafields
(netCDF and/or bpch formats are supported, depending on the backend).


Each backend should be implemented in a Python module named as follows:

    `libname`_backend.py

where `libname` is the name of the python library used as backend.

"""
